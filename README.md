# Permutation-without-repetition-program
A Permutation without repetition means arranging objects where each object can be used only once. No element is repeated in the arrangement, and order matters.  Example: For elements A, B, C, the permutations without repetition are:  ABC ACB BAC BCA CAB CBA  Total permutations = 6
